# benchmarks/

This folder contains PostgreSQL benchmark utilities and requirements.

Files:
- `run_benchmarks.sh` - Benchmark automation script.
- `REQUIREMENTS.md` - Dependencies and setup instructions.

Use the script to run performance tests against a PostgreSQL instance. Review `REQUIREMENTS.md` before running benchmarks.